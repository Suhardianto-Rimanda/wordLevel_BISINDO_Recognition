"""
data_loader.py — Load landmark sequences untuk training.

Tugas: baca X.npy / y.npy dari ``config.LANDMARKS_DIR``, split train/test,
one-hot label.

STUB — logika belum diisi.
"""

# TODO: import numpy as np
# TODO: from sklearn.model_selection import train_test_split
# TODO: import config


def load_dataset(landmarks_dir=None):
    """Kembalikan (X_train, X_test, y_train, y_test)."""
    # TODO: np.load X.npy & y.npy → train_test_split → one-hot
    raise NotImplementedError
