"""
train.py — Loop training LSTM.

Dijalankan TERPISAH di Colab/Kaggle (bukan di laptop RTX 2050).
Tugas: load dataset → build model → fit → simpan .h5 + label_map.json.

STUB — logika belum diisi.
"""

# TODO: from src.training.data_loader import load_dataset
# TODO: from src.training.model import build_lstm_model
# TODO: import config


def main():
    """Entry training: load → build → fit → save."""
    # TODO: load_dataset → build_lstm_model → model.fit(...) → model.save(MODEL_PATH)
    raise NotImplementedError


if __name__ == "__main__":
    main()
